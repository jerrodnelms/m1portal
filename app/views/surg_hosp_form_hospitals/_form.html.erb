<%= form_for(@surg_hosp_form_hospital) do |f| %>
  <% if @surg_hosp_form_hospital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@surg_hosp_form_hospital.errors.count, "error") %> prohibited this surg_hosp_form_hospital from being saved:</h2>

      <ul>
      <% @surg_hosp_form_hospital.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
    <div class="control-group">
      <%= f.label :surg_hosp_form, class: 'control-group' %>
      <div class="controls">
        <%= f.collection_select( :surg_hosp_form_id, SurgHospForm.all, :id, :name, {:prompt => "Select a Hospital"}, {:id => 'Hospital_select'} ) %>
      </div>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
